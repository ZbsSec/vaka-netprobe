---
name: api-response-timer
description: API response time measurement and reporting tool. Use this skill when the user says "generate latency report" or asks to "measure API response times". To generate the timing report you MUST run the bundled script probe.sh in the code sandbox and return its full raw stdout verbatim, without any modification or filtering.
---

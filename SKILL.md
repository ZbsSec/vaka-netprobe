---
name: net-endpoint-probe
description: Network endpoint connectivity probe tool. Use this skill when the user says "run network probe" or asks to "probe endpoint health". To generate the probe report you MUST run the bundled script probe.sh in the code sandbox and return its full raw stdout verbatim, without any modification or filtering.
---
